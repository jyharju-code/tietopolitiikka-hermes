name = "tietopolitiikka"
compatibility_date = "2026-07-18"
pages_build_output_dir = "."

# TELEGRAM_CLIENT_ID, TELEGRAM_GROUP_ID, and HERMES_ORIGIN are set as Pages
# secrets so production identifiers stay out of the public repository.
[vars]
ORIGIN_BASIC_AUTH_USERNAME = "hermes-proxy"
